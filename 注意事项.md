114514


$$M = -k_R e_R - k_\Omega e_\Omega - \mathcal{W}_R \bar{\theta}_R + (R^T R_d \dot{\Omega}_d)^J J R^T R_d \Omega_d  + JR^T R_d \dot{\Omega}_d$$

$$(\dot{\bar{\theta}})_{R}=\gamma_{R}\mathbb{W}_{R}^{\mathrm{T}}(e_{\Omega}+c_{2}e_{R})$$

$k_R$ 姿态比例项

$k_\Omega$ 姿态微分项

$\bar{\theta}_R$ 姿态自适应项

$$f=(k_xe_x+k_\nu e_\nu+\mathbb{W}_x\bar{\theta}_x+mge_3-m\ddot{x}_\mathrm{d})\cdot Re_3$$
$$\begin{aligned}M&=-k_Re_R-k_\Omega e_\Omega-\mathbb{W}_R\bar{\theta}_R+(R^\mathrm{T}R_\mathrm{c}\Omega_\mathrm{c})^\wedge JR^\mathrm{T}R_\mathrm{c}\Omega_\mathrm{c}\\&+JR^\mathrm{T}R_\mathrm{c}\dot{\Omega}_\mathrm{c}\end{aligned}$$

$$(\dot{\bar{\theta}})_x=\begin{cases}\gamma_x\mathbb{W}_x^\mathrm{T}(e_\nu+c_1e_x)&\text{if}\left\|\bar{\theta}_x\right\|<B_\theta\\&\text{or}\left\|\bar{\theta}_x\right\|=B_\theta\\&\text{and}\bar{\theta}_x^\mathrm{T}\mathbb{W}_x^\mathrm{T}(e_\nu+c_1e_x)\leq0\\\\\gamma_x\left(I-\frac{\bar{\theta}_x\bar{\theta}_x^\mathrm{T}}{\bar{\theta}_x^\mathrm{T}\bar{\theta}_x}\right)\mathbb{W}_x^\mathrm{T}(e_\nu+c_1e_x)&\text{Otherwise}\end{cases}$$

$k_x$ 位置比例项

$k_\nu$ 位置微分项

$\bar{\theta}_R$ 位置自适应项

trajectory_num
   
    case 0:
        Trajectory_Generate_POS
    case 1:
        Trajectory_Generate_POS_AUTO
    case 2:
        Trajectory_Generate_CIRCLE_AUTO
    case 3:
        Trajectory_Generate_EIGHT_AUTO
    case 4:
        Trajectory_Generate_LSR_AUTO
    case 5:
        // circular trajectory with variable yaw
        ACRL_trajectory_circle_variable_yaw
    case 6:
        // circular trajectory with fixed yaw
        ACRL_trajectory_circle_fixed_yaw
    case 7: 
        // figure8 trajectory with fixed yaw
        ACRL_trajectory_figure8_fixed_yaw
    case 8: 
        // cy45 add: figure8 trajectory with tilted altitude
        ACRL_trajectory_figure8_tilted
    default:
        Trajectory_Generate_POS